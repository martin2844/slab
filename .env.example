# Slab Configuration

# REST API
TRACKER_PORT=3000
TRACKER_API_KEY=change-me-to-a-secret-key

# MCP Server
TRACKER_MCP_PORT=3001
TRACKER_MCP_MODE=http    # "http" for remote access, "stdio" for local CLI

# Database (defaults to ./slab.db)
TRACKER_DB_PATH=./slab.db
